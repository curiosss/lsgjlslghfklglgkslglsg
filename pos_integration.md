in backend create pos (point of sale integration)  requests, 2 post request, as shown in test_server. we tested it, for authorization there should be /token/ post request , pos server sends username and password , and in admin we can be able create username and password for it, make separate sidebar item as pos integration , and there we create pos users their username and password, based on that in /token/ request we send them access token, after pos server athorizes, he then can send us products , with token also, in /import-product/ request post request, in which products created, but status will be not reviewed, we need also add statuses to products, and also in admin separate sidebar and window for notreviewed products, admin sees it, if he wants adds photo, add fill names and other field in different languages, also in products create field pos_name , there is know name_en, and when creting product from pos_server when posting, fill the name_en name_ru by default with pos_name, in admin the pos_name also should be shown above name inputs 